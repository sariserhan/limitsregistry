import { describe, expect, it } from "vitest";
import { isPublicBounty, validateBountyInput } from "./bounties";
const valid = { title: "P versus NP prize", sponsor: "Example Foundation", description: "A verified prize for resolving the stated open problem.", sourceUrl: "https://example.org/prize" };
describe("bounty validation", () => { it("accepts strict complete records", () => expect(validateBountyInput({ ...valid, amount: "1000000.00", currency: "USD", expiresAt: new Date("2030-01-01") }, new Date("2026-01-01"))).toBeNull()); it("rejects invalid source, money pairs, and past expiration", () => { expect(validateBountyInput({ ...valid, sourceUrl: "http://example.org" })).toMatch(/HTTPS/); expect(validateBountyInput({ ...valid, amount: "10" })).toMatch(/together/); expect(validateBountyInput({ ...valid, expiresAt: new Date("2020-01-01") })).toMatch(/future/); }); it("publishes only unexpired verified records", () => { expect(isPublicBounty("VERIFIED", null)).toBe(true); expect(isPublicBounty("UNVERIFIED", null)).toBe(false); expect(isPublicBounty("VERIFIED", new Date("2020-01-01"), new Date("2026-01-01"))).toBe(false); }); });
