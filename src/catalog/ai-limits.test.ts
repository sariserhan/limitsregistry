import{describe,expect,it}from"vitest";import{aiLimitRecords}from"./ai-limits";describe("AI limits catalog",()=>{it("contains more than 30 unique source-backed records",()=>{expect(aiLimitRecords.length).toBeGreaterThan(30);expect(new Set(aiLimitRecords.map(r=>r.title)).size).toBe(aiLimitRecords.length);for(const r of aiLimitRecords){expect(r.source.url).toMatch(/^https:/);expect(Object.keys(r.constraints).length).toBeGreaterThan(4)}});it("keeps empirical scaling fits open",()=>{expect(aiLimitRecords.filter(r=>r.status==="OPEN").map(r=>r.title)).toEqual(["Chinchilla compute-optimal language-model frontier","Kaplan language-model scaling frontier"])});it("does not label source-confirmed estimates proven",()=>{for(const r of aiLimitRecords.filter(r=>r.evidenceBasis==="ESTIMATE")){expect(r.status).toBe("OPEN");expect(r.epistemic).toBe("SOURCE_CONFIRMED")}})});
