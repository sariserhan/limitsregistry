import { NextResponse } from "next/server";
import { listPublishedDomainLimits } from "../../../src/db/repository";
import { allowRequest } from "../../../src/ops/rate-limit";
export const runtime = "nodejs";
export async function GET(request: Request) { const ip = request.headers.get("x-forwarded-for") ?? "unknown"; if (!allowRequest(`export:${ip}`)) return NextResponse.json({ error: "Rate limit exceeded." }, { status: 429 }); try { const data = await listPublishedDomainLimits(); const format = new URL(request.url).searchParams.get("format"); if (format === "csv") { const header = "registryNumber,title,category,status\n"; const rows = data.map(item => [item.id,item.title,item.category,item.status].map(value => `"${String(value).replaceAll('"','""')}"`).join(",")).join("\n"); return new Response(header + rows + "\n", { headers: { "content-type": "text/csv; charset=utf-8", "content-disposition": "attachment; filename=limits-registry.csv" } }); } return NextResponse.json({ data, generatedAt: new Date().toISOString() }); } catch { return NextResponse.json({ error: "Registry export unavailable." }, { status: 503 }); } }
