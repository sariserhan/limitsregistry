import { getPublishedLimit } from "../../../../src/db/repository";
import { listWatchlistEvents } from "../../../../src/db/repository.research";
export const runtime = "nodejs"; export const revalidate = 300;
const xml = (v: string) => v.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;").replace(/'/g, "&apos;");
export async function GET(request: Request) { const limitId = new URL(request.url).searchParams.get("limitId"); if (!limitId) return new Response("limitId is required", { status: 400 }); const limit = await getPublishedLimit(limitId); if (!limit) return new Response("Not found", { status: 404 }); const events = await listWatchlistEvents(limit.id); const items = events.map(e => `<item><title>${xml(e.eventType)}</title><description>${xml(JSON.stringify(e.payload))}</description><pubDate>${e.createdAt.toUTCString()}</pubDate><guid>${e.id}</guid></item>`).join(""); return new Response(`<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"><channel><title>${xml(limit.title)} — Limits Registry</title><link>https://www.limitsregistry.com/limits/${xml(limit.registryNumber)}</link><description>Published research events for ${xml(limit.title)}</description>${items}</channel></rss>`, { headers: { "content-type": "application/rss+xml; charset=utf-8", "cache-control": "public, max-age=300" } }); }
